with Tropos;

package Carthage.Houses.Configure is

   procedure Configure_House
     (Config : Tropos.Configuration);

   procedure Configure_Start
     (House        : House_Type;
      Start_Config : Tropos.Configuration);

end Carthage.Houses.Configure;
